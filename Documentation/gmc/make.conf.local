FEATURES="sandbox -buildpkg ccache -distcc distlocks -strict -digest parallel-fetch -splitdebug"

USE="$USE sse sse2 sse3 ssse3 -lirc -lm_sensors -cpudetection mmx mmxext -cups -dri vdpau -pvr samba -semantic-desktop systemd"
MAKEOPTS="-j2"
INPUT_DEVICES="evdev"
CAMERAS="ptp2"

CFLAGS="-march=core2 -O2 -pipe"
CXXFLAGS="${CFLAGS} -fpermissive -Wno-write-strings -Wno-deprecated"
